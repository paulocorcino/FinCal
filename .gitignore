# Ralphy run artifacts (scratch, logs, per-run plans)
.ralphy/
.agents/

# Env / secrets (NUNCA commitar tokens)
.env
.env.local
.env.*.local

# dbkit build output (derived — never source)
dbkit/generated/
# dbkit local work area (adoption ledger, inventories)
.scratch/